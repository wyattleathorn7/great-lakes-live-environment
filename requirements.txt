# Pinned floors keep fresh installs working; no upper bounds so routine
# security/bugfix updates keep flowing. Verified with these versions.
numpy>=1.26
pillow>=10.1
eccodes>=2.30
pyshp>=2.3
shapely>=2.0
pyproj>=3.6
